# Loaded by `npm run dev:api` (vite --mode integration). Talks to the real Spring backend:
# MSW is off, and same-origin /api calls are proxied to VITE_PROXY_TARGET (no CORS needed).
VITE_USE_MSW=false
VITE_PROXY_TARGET=http://localhost:8080
